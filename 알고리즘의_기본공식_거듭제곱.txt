거듭제곱하는 방법

(1)반복문을 이용하기
power_of_2( k ) // Output: 2k
	i ← 0
	power ← 1
	WHILE i < k
		power ← power * 2
		i++
	RETURN power

(2)재귀호출 이용하기
power_of_2( k ) // Output: 2k
	if k == 0
		return 1
	else
		return 2 * power_of_2(k - 1)
		
