structure (Point) {
  description (위도, 경도를 저장하는 geo 캡슐의 NamedPoint를 복제하였습니다)
  // 참조 (https://bixbydevelopers.com/dev/docs/dev-guide/developers/library.geo#points)
  role-of (geo.NamedPoint)
  //  can give a concept the `role-of` another concept 
  // to change its behavior based solely on context.
  // 특정 라이브러리에서 불러올때, 
  // 그 라이브러리가 가지는 concept의 사용방법을 맥락에 따라 바꾸기 위해 role-of 지정
}
